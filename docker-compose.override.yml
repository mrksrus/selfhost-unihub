# Development override – automatically loaded by `docker compose up`.
# This tells Compose to BUILD from source instead of pulling from ghcr.io.
# Self-hosters who only have docker-compose.yml will pull pre-built images.

services:
  unihub-app:
    build:
      context: .
      dockerfile: Dockerfile

  unihub-api:
    build:
      context: .
      dockerfile: Dockerfile.api
